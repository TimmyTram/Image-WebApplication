<div id="post-{{id}}" class="card">
    <img src={{thumbnail}} alt="Missing Image.">
    <div class="card-body">
        <p class="card-title">{{title}}</p>
        <p class="card-text">{{description}}</p>
        <a href="/getPost/{{id}}" class="anchor-buttons"><i class="fas fa-info-circle"></i> Post Details</a>
    </div>
</div>